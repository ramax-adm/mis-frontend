import { FormActionButtons } from './actionButtons'
import { FormCloseButton } from './closeButton'
import { FormInput } from './input'
import { FormRoot } from './root'
import { FormSubmitButton } from './submitButton'

export const Form = {
  Root: FormRoot,
  Input: FormInput,
  Action: FormActionButtons,
  CloseButton: FormCloseButton,
  SubmitButton: FormSubmitButton,
}
